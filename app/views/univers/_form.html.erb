<%= form_for(@univer) do |f| %>
  <% if @univer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@univer.errors.count, "error") %> prohibited this univer from being saved:</h2>

      <ul>
      <% @univer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
